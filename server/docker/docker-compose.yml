version: '3'

services:
  mockserver:
    container_name: release-management-service-ut-mockserver
    image: ${DOCKER_REGISTRY}/mockserver/mockserver:mockserver-${MOCKSERVER_VERSION}
    ports:
      - "${TEST_MOCKSERVER_PORT:-1080}:1080"
