name: Merge Gate

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  build:
    uses: ./.github/workflows/build.yml

  quality:
    uses: ./.github/workflows/quality.yml

  security:
    uses: ./.github/workflows/security.yml

  gate-merge:
    name: gate/merge
    if: ${{ always() }}
    needs: [build, quality, security]
    runs-on: ubuntu-latest
    steps:
      - uses: octopusden/octopus-base/.github/actions/merge-gate@9f693dc5c38c56a13ab7a81b0d7d3a60a1382c75
        with:
          needs: ${{ toJson(needs) }}
