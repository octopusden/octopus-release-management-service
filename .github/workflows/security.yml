name: Security Reports

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 3 * * *"
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  security:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-security-reports.yml@v2.3.0
    with:
      java-version: "21"
      enable-codeql: true
      enable-trivy: true
      # TODO: Re-enable after dependency-check stability is fixed (NVD API/source reliability).
      enable-dependency-check: false
      # Report-only: transient CodeQL/Trivy infra failures must not block merge.
      codeql-continue-on-error: true
      trivy-continue-on-error: true
